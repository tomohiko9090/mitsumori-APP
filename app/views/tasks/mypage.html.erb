

<%# time = Time.now これは本番時に当日の日時を実装する 実装%>
<%= area_chart Task.where(user_id: @user.id).where.not(action_date: [nil, "2022/08/14"]).group(:action_date).order(:action_date).sum(:actual_time), colors: ["#76B852"], ytitle: "実働時間(min)", label: "実績時間(min)" %>

<div style="text-align:center">
    <%= link_to :タスク一覧へ, tasks_path, class: "bubbly-button" %>
</div>